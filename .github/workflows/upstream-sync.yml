---
name: Upstream sync check
'on':
  schedule:
    - cron: "0 6 * * 1"  # weekly, Monday
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  check:
    name: Check geerlingguy/ansible-role-logstash for changes
    runs-on: ubuntu-latest
    steps:
      - name: Check out this repo
        uses: actions/checkout@v4
        with:
          path: logstash

      - name: Check out upstream geerlingguy/ansible-role-logstash
        uses: actions/checkout@v4
        with:
          repository: geerlingguy/ansible-role-logstash
          path: upstream

      # handlers/main.yml, tasks/ssl.yml and tasks/plugins.yml are kept
      # byte-for-byte identical to upstream (see README.md's Provenance
      # section), so a straight diff is enough to know whether a sync is due.
      - name: Compare the files carried verbatim from upstream
        id: diff
        working-directory: logstash
        run: |
          changed=0
          for f in handlers/main.yml tasks/ssl.yml tasks/plugins.yml; do
            if ! diff -q "$f" "../upstream/$f" >/dev/null 2>&1; then
              echo "Changed upstream: $f"
              changed=1
              cp "../upstream/$f" "$f"
            fi
          done
          echo "changed=$changed" >> "$GITHUB_OUTPUT"

      - name: Open or update a PR with the upstream changes
        if: steps.diff.outputs.changed == '1'
        working-directory: logstash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          upstream_sha=$(git -C ../upstream rev-parse --short HEAD)
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b upstream-sync
          git add handlers/main.yml tasks/ssl.yml tasks/plugins.yml
          git commit -m "Sync verbatim upstream files from geerlingguy/ansible-role-logstash@${upstream_sha}"
          git push -u origin upstream-sync --force
          if ! gh pr list --head upstream-sync --state open --json number --jq '.[0]' | grep -q .; then
            gh pr create \
              --title "Upstream sync: geerlingguy/ansible-role-logstash@${upstream_sha}" \
              --body "handlers/main.yml, tasks/ssl.yml and/or tasks/plugins.yml changed upstream at geerlingguy/ansible-role-logstash@${upstream_sha}. These are meant to stay byte-for-byte identical to upstream (see README.md), so review the diff before merging in case the change needs adapting for this fork." \
              --base master --head upstream-sync
          fi
