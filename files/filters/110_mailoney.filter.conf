# Filter Mailoney messages from filebeat
filter {
  if "Mailoney" in [tags] {
    date {
      match => [ "timestamp", "ISO8601" ]
    }
    mutate {
      add_field => {
        "dest_port" => "25"
      }
    }
  }
}
