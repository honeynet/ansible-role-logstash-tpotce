# Filter Glutton messages from filebeat
filter {
  if "glutton" in [tags] {
    json {
      source => "message"
    }
    date {
      match => [ "ts", "UNIX" ]
      remove_field => ["ts"]
    }
  }
}
