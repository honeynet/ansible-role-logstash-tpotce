# Adbhoney logs
output {
  if "Adbhoney" in [tags] and "fails" not in [tags] {
    elasticsearch {
      hosts => {{ logstash_elasticsearch_hosts | to_json }}
      manage_template => false
      index => "adbhoney-%{+YYYY.MM.dd}"
    }
  }
}
