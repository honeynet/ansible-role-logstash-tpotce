# Rdpy logs
output {
  if "Rdpy" in [tags] and "fails" not in [tags] {
    elasticsearch {
      hosts => {{ logstash_elasticsearch_hosts | to_json }}
      manage_template => false
      index => "rdpy-%{+YYYY.MM.dd}"
    }
  }
}
