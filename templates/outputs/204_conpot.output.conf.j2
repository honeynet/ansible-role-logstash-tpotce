# ConPot logs
output {
  if "ConPot" in [tags] and "fails" not in [tags] {
    elasticsearch {
      hosts => {{ logstash_elasticsearch_hosts | to_json }}
      manage_template => false
      index => "logstash-conpot-%{+YYYY.MM.dd}"
    }
  }
}
