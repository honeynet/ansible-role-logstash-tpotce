# Log4pot logs
output {
  if "log4pot" in [tags] and "fails" not in [tags] {
    elasticsearch {
      hosts => {{ logstash_elasticsearch_hosts | to_json }}
      manage_template => false
      index => "logstash-log4pot-%{+YYYY.MM.dd}"
    }
  }
}
