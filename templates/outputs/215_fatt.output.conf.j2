# Fatt logs
output {
  if "Fatt" in [tags] and "fails" not in [tags] {
    elasticsearch {
      hosts => {{ logstash_elasticsearch_hosts | to_json }}
      manage_template => false
      index => "logstash-fatt-%{+YYYY.MM.dd}"
    }
  }
}
