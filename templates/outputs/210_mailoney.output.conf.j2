# Mailoney logs
output {
  if "Mailoney" in [tags] and "fails" not in [tags] {
    elasticsearch {
      hosts => {{ logstash_elasticsearch_hosts | to_json }}
      manage_template => false
      index => "mailoney-%{+YYYY.MM.dd}"
    }
  }
}
